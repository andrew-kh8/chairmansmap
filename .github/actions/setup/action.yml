name: 'Setup environment'
description: 'Install system dependencies'

runs:
  using: "composite"
  steps:
    - name: Install geo utils
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install libproj-dev proj-bin